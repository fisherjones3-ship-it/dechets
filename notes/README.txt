Each note is one text file. Name it with the date first:

  2026-09-14-whatever-you-like.txt

The FIRST line of the file is the title.
Everything after it is the body. Leave a blank line between paragraphs.
Newest date shows at the top automatically.
